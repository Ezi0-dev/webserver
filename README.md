### Webserver made in C#

Fully working GET, POST, PUT, DELETE with error and logging middleware

Gets params like {id} from the TryMatchRoute helper function

Uses postgres to store user data

Really basic for now, but might build more on it later on.

If i do the next step would probably be to add some sort auth like JWT

